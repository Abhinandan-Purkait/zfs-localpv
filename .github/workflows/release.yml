name: Release Images and Charts

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+**'
jobs:
  pre-checks:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v22
      - name: Pre-populate nix-shell
        run: |
          export NIX_PATH=nixpkgs=$(jq '.nixpkgs.url' nix/sources.json -r)
          echo "NIX_PATH=$NIX_PATH" >> $GITHUB_ENV
          nix-shell --pure --run "echo" ./shell.nix

      - name: Mirror multi-arch image with crane
        env:
          SRC: ghcr.io/${{ env.IMAGE_ORG }}/dev/zfs-driver:${{ github.ref_name }}
          DH: docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_ORG }}/zfs-driver:${{ github.ref_name }}
        run: |
            nix-shell --pure --run 'crane auth login -u ${{ secrets.DOCKERHUB_USERNAME }} --password ${{ secrets.DOCKERHUB_TOKEN }}'
            nix-shell --pure --run 'crane copy --platform all "$SRC" "$DH"'

  helm:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Helm CLI
        uses: azure/setup-helm@v3

      - name: Log in to GHCR for Helm
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u abhinandan-purkait --password-stdin

      - name: Pull OCI chart from GHCR
        run: |
          CHART_REF=oci://ghcr.io/abhinandan-purkait/helm/zfs-localpv
          mkdir -p charts-staged
          helm pull "$CHART_REF" \
            --version "${{ github.ref_name }}" \
            --destination charts-staged
          mkdir -p charts-final
          tar -xvf charts-staged/zfs-localpv-v2.11.0.tgz -C charts-final

      - name: Publish chart via helm-gh-pages
        uses: stefanprodan/helm-gh-pages@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          charts_dir: ./charts-final

# skopeo copy \
#   --src-creds "${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}" \
#   --dest-creds "${{ secrets.QUAY_USERNAME }}:${{ secrets.QUAY_TOKEN }}" \
#   "$SRC" "$QY_DEST"

# skopeo copy \
#   --src-creds "${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}" \
#   --src-creds "${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}" \
#   "$SRC" "$GH_DEST"
